{
  "name": "fishtrack-root",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm run initdb --prefix backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
